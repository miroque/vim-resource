"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""" то что я могувключить без плагинов, чисто саму конфигурацию
"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
set nocompatible		" be iMproved
set number              " включить нумерацию строк
set relativenumber      " включить реверсивную нумерацию строк
syntax on               " включить подсветку синтаксиса
set tabstop=4           " размер табулятора
set autoindent          " автоматический отступ
set cursorline          " подсветка линии где курсор стоит
"set expandtab          " это меняет табы на пробелы
"set softtabstop=4      " это удаление пробелов бекспейсом, словно табулятор удалили, должно коррелироваться с параметном tabstop
"colorscheme gruvbox    " выбор цветовой схемы

set laststatus=2        " это две линнии статуса, чтоб выбо видндо всегда название файла и так далее
set colorcolumn=120     " это подсвечивает колонку, которую можно контроллировать сбея и не переходить её
set foldmethod=indent   " выставляет режим схлопывания по отсупам
set shiftwidth=4        " указывает что отступ равен 4, как при табуляции  1 set foldmethod=indent




"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
""" маппинг микро-команд
"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
:nnoremap <F5> "=strftime("\|%H:%M (%z)\|")<CR>P
:inoremap <F5> <C-R>=strftime("\|%H:%M (%z)\|\n")<CR>



"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
"""
"""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
